metadata:
  name: {{id}}
  display_name: "{{displayName}}"
  version: "{{version}}"
  domain: {{domain}}
  status: active

description: |
  {{description}}

agents:
{{agentEntries}}

tags:
  - emulated
  - {{domain}}
